name: avas_transfer
description: BML Avas Transfer

publish_to: 'none'

version: 1.0.3+3

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  http: ^0.12.1
  intl: ^0.16.1
  shared_preferences: ^0.5.10
  sms: ^0.2.4
  permission_handler: ^5.1.0+2
  path_provider: ^1.6.24
  share_extend: ^1.1.9
  flutter_barcode_scanner: ^1.0.2
  flutter_mobile_vision: ^0.1.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.8.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/